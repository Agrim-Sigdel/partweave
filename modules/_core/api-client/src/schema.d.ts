/**
 * Placeholder OpenAPI types. Regenerate the real ones from the running server:
 *
 *   make gen-api        # or: pnpm --filter @app/api-client generate
 *
 * (reads $API_SCHEMA_URL, default http://localhost:8000/api/schema/)
 */
export interface paths {}
export interface components {
  schemas: Record<string, unknown>;
}
export interface operations {}
