# The generator's own packages, plus partweave's own apps (the website), are
# real workspace members. Everything under modules/ is TEMPLATE SOURCE (copied
# into generated projects), NOT installed here — deliberately excluded.
packages:
  - "packages/*"
  - "apps/*"
