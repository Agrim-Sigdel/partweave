{
  "name": "quick-build-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "A modular full-stack scaffolding platform — pick the parts you need (Django server, Next.js web, Expo mobile) and generate only that code, wired together.",
  "type": "module",
  "packageManager": "pnpm@10.20.0",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm --filter @agrimsigdel/quick-build build",
    "dev": "pnpm --filter @agrimsigdel/quick-build dev",
    "cli": "pnpm --filter @agrimsigdel/quick-build exec quick-build",
    "create": "pnpm --filter @agrimsigdel/quick-build exec create-quick-build",
    "test": "pnpm --filter @agrimsigdel/quick-build test",
    "typecheck": "pnpm -r typecheck",
    "release": "pnpm --filter @agrimsigdel/quick-build exec npm version"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
